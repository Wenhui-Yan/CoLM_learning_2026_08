## 1.环境复查
```bash

```
## 2.LSF集群bsub常用作业调度命令速查
```bash
bjobs -l 作业编号     # 查看作业完整资源、节点、输出路径、异常报错信息
bjobs                # 查看自己正在排队/运行的作业
bkill 0              # 杀掉自己全部作业
bkill 作业编号        # 杀掉该作业
```


## 附录：Vim 常用操作
```bash
gg          # 跳到文件开头
G           # 跳到文件末尾
i           # 进入插入模式
Esc         # 退出插入模式
:wq         # 保存并退出
:q!         # 强制退出不保存
/关键词      # 搜索关键词
fg %编号     # 恢复后台暂停的进程
```
