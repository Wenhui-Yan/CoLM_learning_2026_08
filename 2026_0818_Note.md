
修改宏定义(define.h) → 重新编译(make) → 复制站点数据 → 配置Forcing Namelist → 配置Run Namelist → 提交作业 → 查看结果

mksrfdata.x  →  制作地表数据
     ↓
mkinidata.x  →  制作初始场
     ↓
colm.x       →  运行模式模拟

登录
ssh colm099@211.66.130.146
ssh node112
加载编译器
source /share/home/colm150/software/installed/intel/oneapi/setvars.sh
环境验证
which ifort
ifort --version
which mpif90
# 1 修改宏定义文件（define.h）
## 1.1 进入 include 目录并编辑 define.h
```bash
cd ~/CoLM-CoLM2024-Tutorial/include
vi define.h
```
## 1.2 自然地表单点模拟的宏定义设置
```bash

```
# 2 重新编译模型
```bash
cd ~/CoLM-CoLM2024-Tutorial
make clean          # 清理之前的编译文件
make                # 编译模型
ls -l run/*.x       # 检查编译是否成功（应有 mksrfdata.x、mkinidata.x、colm.x）
```

## 3 准备站点数据文件
```bash
#3.1 进入 run 目录
cd ~/CoLM-CoLM2024-Tutorial/run
#3.2 复制站点观测数据
cp /share/home/dq089/training2026/PLUMBER2s/Sitedata/IT-Isp_2013-2014_FLUXNET2015_site.nc ./

#3.3 复制 Forcing Namelist
cp /share/home/dq089/training2026/PLUMBER2s/Forcingnml/IT-Isp.nml ./

# 复制 Run Namelist
cp /share/home/dq089/training2026/IT-Isp/Site_IT-Isp.nml ./
vi define.h
```
## 4.1 配置 Forcing Namelist
```bash
vi IT-Isp.nml
```
每次退出重启都要
source /share/home/colm150/.bashrc

&nl_colm_forcing                                 ! Namelist 分组名，固定写法

   DEF_dir_forcing = '/share/home/dq089/training2026/PLUMBER2s/Forcing/'   ! 大气强迫数据存放目录

   DEF_forcing%dataset           = 'POINT'        ! 数据集类型：单点（POINT）/全球（GSWP3等）
   DEF_forcing%solarin_all_band  = .true.         ! 太阳辐射是否全波段（true=全波段，false=分波段）
   DEF_forcing%HEIGHT_V          = 40.0           ! 风速观测高度（m）
   DEF_forcing%HEIGHT_T          = 40.0           ! 温度观测高度（m）
   DEF_forcing%HEIGHT_Q          = 40.0           ! 比湿观测高度（m）

   DEF_forcing%NVAR              = 8              ! 强迫变量总数（固定为8）
   DEF_forcing%startyr           = 2013           ! 强迫数据起始年份
   DEF_forcing%startmo           = 1              ! 强迫数据起始月份
   DEF_forcing%endyr             = 2014           ! 强迫数据结束年份
   DEF_forcing%endmo             = 12             ! 强迫数据结束月份
   DEF_forcing%dtime             = 1800 1800 1800 1800 1800 1800 1800 1800   ! 每个变量时间步长（秒）
   DEF_forcing%offset            = 0 0 0 0 0 0 0 0   ! 每个变量时间偏移量（秒）

   DEF_forcing%leapyear          = .true.         ! 是否使用闰年历法（true=使用）
   DEF_forcing%data2d            = .false.        ! 数据是否为二维（lat,lon）（单点为false）
   DEF_forcing%hightdim          = .false.        ! 是否有垂直维度（单点为false）
   DEF_forcing%dim2d             = .false.        ! 经纬度是否为二维数组（单点为false）

   DEF_forcing%groupby           = 'none'         ! 文件分组方式：none（单文件）/year（按年）/month（按月）/yearmonth（按年月）

   DEF_forcing%fprefix(1) = 'IT-Isp_2013-2014_FLUXNET2015_Met.nc'   ! 变量1文件名：温度（Tair）
   DEF_forcing%fprefix(2) = 'IT-Isp_2013-2014_FLUXNET2015_Met.nc'   ! 变量2文件名：比湿（Qair）
   DEF_forcing%fprefix(3) = 'IT-Isp_2013-2014_FLUXNET2015_Met.nc'   ! 变量3文件名：气压（Psurf）
   DEF_forcing%fprefix(4) = 'IT-Isp_2013-2014_FLUXNET2015_Met.nc'   ! 变量4文件名：降水（Precip）
   DEF_forcing%fprefix(5) = 'IT-Isp_2013-2014_FLUXNET2015_Met.nc'   ! 变量5文件名：NULL（占位符，未使用）
   DEF_forcing%fprefix(6) = 'IT-Isp_2013-2014_FLUXNET2015_Met.nc'   ! 变量6文件名：风速（Wind）
   DEF_forcing%fprefix(7) = 'IT-Isp_2013-2014_FLUXNET2015_Met.nc'   ! 变量7文件名：向下短波辐射（Swdown）
   DEF_forcing%fprefix(8) = 'IT-Isp_2013-2014_FLUXNET2015_Met.nc'   ! 变量8文件名：向下长波辐射（Lwdown）

   DEF_forcing%vname    = 'Tair' 'Qair' 'Psurf' 'Precip' 'NULL' 'Wind' 'Swdown' 'Lwdown'   ! NetCDF文件中对应的变量名
   DEF_forcing%timelog  = 'instant' 'instant' 'instant' 'forward' 'NULL' 'instant' 'forward' 'forward'   ! 时间记录类型：instant（瞬时）/forward（累积）
   DEF_forcing%tintalgo = 'linear' 'linear' 'linear' 'nearest' 'NULL' 'linear' 'linear' 'linear'   ! 时间插值算法：linear（线性）/nearest（最近邻）/coszen（余弦天顶角）

/                                                                      ! Namelist结束标记（必须有！）
